# Проект по дисциплине «Дополнительные главы алгоритмики»

Данный репозиторий содержит набор реализованных задач по дисциплине  
**«Дополнительные главы алгоритмики»**.

Все задачи написаны на языке **Java** и структурированы по тематическим разделам.  
Для **каждой задачи** в отдельной папке расположен:
- файл с реализацией (`.java`),
- файл с описанием решения (`readme.md`).

Проект формировался в течение семестра по мере изучения соответствующих тем.

---

## 📂 Общая структура проекта

com/smesitejl<br>
├── Basic <br>
├── Cryptography<br>
├── Dynamic<br>
├── Geometry<br>
├── Graph<br>
├── Greedy<br>
├── Numbertheory<br>
├── Poset<br>
└── readme.md<br>


---

## 📁 Basic — базовые и демонстрационные задачи

Basic<br>
├── FractionalKnapsack<br>
├── HarmonicLowerBound<br>
├── HistogramPanel<br>
├── MaxAssignments<br>
└── XOR<br>


- `FractionalKnapsack` — задача о непрерывном рюкзаке (жадный алгоритм)
- `HarmonicLowerBound` — оценка снизу для гармонического ряда
- `HistogramPanel` — построение гистограммы распределения
- `MaxAssignments` — подсчёт операций присваивания при поиске максимума
- `XOR` — обмен значений с использованием операции XOR

---

## 🔐 Cryptography — криптография

Cryptography<br>
├── RSAExample.java<br>
└── readme.md<br>


- `RSAExample` — реализация алгоритма RSA:  
  генерация ключей, шифрование, дешифрование, цифровая подпись и её проверка

---

## 🔁 Dynamic — динамическое программирование

Dynamic<br>
├── GlassBalls<br>
├── LCS<br>
└── VaccineProduction<br>


- `GlassBalls` — задача о стеклянных шариках
- `LCS` — наибольшая общая подпоследовательность
- `VaccineProduction` — задача оптимизации производства вакцины

---

## 📐 Geometry — вычислительная геометрия

Geometry<br>
├── PolygonArea<br>
├── TrapezoidPackingCenter<br>
└── TrapezoidPackingMiddle<br>


- `PolygonArea` — площадь невыпуклого многоугольника
- `TrapezoidPackingCenter` — безотходная укладка трапеций (вращение относительно центра)
- `TrapezoidPackingMiddle` — безотходная укладка трапеций (вращение относительно средней линии)

---

## 🧠 Graph — алгоритмы на графах

Graph<br>
├── CurrencyArbitrage<br>
├── DominoTiling<br>
├── GraphCenterMedian<br>
├── Kruskal<br>
└── Matchmaker<br>

- `CurrencyArbitrage` — поиск валютного арбитража (отрицательные циклы)
- `DominoTiling` — укладка домино как задача о двудольном паросочетании
- `GraphCenterMedian` — поиск центра и медианы ориентированного взвешенного графа
- `Kruskal` — минимальное остовное дерево (алгоритм Краскала)
- `Matchmaker` — задача о свахе (двудольное паросочетание)

---

## 🟡 Greedy — жадные алгоритмы
Greedy<br>
├── ExamSchedule<br>
└── Hotel<br>


- `ExamSchedule` — задача о зачётной сессии (минимизация штрафа)
- `Hotel` — интервальное планирование заявок в гостинице

---

## 🔢 numbertheory — теория чисел и комбинаторика

Numbertheory<br>
├── DiophantineSearch<br>
├── KazanHappyCount15<br>
├── KazanHappyTicket<br>
├── MoscowPiterHappyTickets<br>
└── PrimalityTests<br>


- `DiophantineSearch` — линейные диофантовы уравнения
- `KazanHappyCount15` — количество 15-значных казанских счастливых билетов
- `KazanHappyTicket` — проверка казанского счастливого билета
- `MoscowPiterHappyTickets` — счастливые билеты по московскому и питерскому определениям
- `PrimalityTests` — тест Ферма, Миллера–Рабина, числа Кармайкла

---

## 🔗 poset — частичные порядки и разбиения

Poset<br>
├── LogGraphs<br>
└── QuadraticGraphs<br>


- `LogGraphs` — минимальное разбиение на монотонные цепи за O(n log n)
- `QuadraticGraphs` — квадратичное разбиение за O(n²)

---

## 🧑‍💻 Язык и технологии

- Язык программирования: **Java**
- Используемые средства:
  - стандартная библиотека Java
  - `BigInteger`
  - стандартные структуры данных (`ArrayList`, `HashMap`, `PriorityQueue`, DSU и др.)

---

## 📌 Примечание

Каждая задача реализовывалась отдельно с анализом:
- корректности,
- выбора алгоритма,
- асимптотической сложности.

Все пояснения приведены в соответствующих папках задач.
