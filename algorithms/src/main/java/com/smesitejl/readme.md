# Доп. главы алгоритмики

Данный репозиторий содержит набор решённых задач по дисциплине **«Дополнительные главы алгоритмики»**.  
Все задачи реализованы на языке **Java** и сгруппированы по тематическим разделам: жадные алгоритмы, динамическое программирование, графы, геометрия, теория чисел, криптография, частичные порядки и др.

Проект формировался в течение семестра по мере изучения соответствующих тем.  
Для ряда задач дополнительно подготовлены пояснительные материалы в формате `.md`.

---

## 📂 Структура проекта

├─ basic
├─ cryptography
├─ dynamic
├─ geometry
├─ graph
├─ greedy
├─ numbertheory
└─ poset



Ниже приведено описание каждого раздела и перечень реализованных в нём задач.

---

## 🧩 basic — базовые и экспериментальные задачи

Задачи из начального блока курса, связанные с анализом алгоритмов, простыми жадными стратегиями и экспериментальными измерениями.

- `FractionalKnapsack` — задача о непрерывном рюкзаке (жадный алгоритм, O(n log n))
- `HarmonicLowerBound` — оценка снизу для гармонического ряда
- `HistogramPanel` — экспериментальная визуализация распределений
- `MaxAssignments` — подсчёт количества операций при поиске максимума
- `XOR` — обмен значений с использованием операции XOR

---

## 🔐 cryptography — криптография

Задачи, связанные с основами криптографии и модульной арифметикой.

- `RSAExample` — реализация алгоритма RSA: генерация ключей, шифрование, дешифрование, цифровая подпись и проверка подписи

---

## 🔁 dynamic — динамическое программирование

Классические и усложнённые задачи на динамическое программирование.

- `LCS` — наибольшая общая подпоследовательность
- `VaccineProduction` — задача о производстве вакцины (динамическое программирование)
- `GlassBalls` — задача о стеклянных шариках (биномиальная формула + бинарный поиск)

---

## 📐 geometry — вычислительная геометрия

Задачи на работу с геометрическими объектами и вычисление площадей.

- `PolygonArea` — вычисление площади невыпуклого многоугольника
- `TrapezoidPackingCenter` — безотходная укладка трапеций с вращением относительно центра
- `TrapezoidPackingMiddle` — безотходная укладка трапеций с вращением относительно средней линии

---

## 🧠 graph — алгоритмы на графах

Задачи на ориентированные и неориентированные графы, паросочетания, остовные деревья, центры и медианы.

- `CurrencyArbitrage` — поиск возможности валютного арбитража
- `DominoTiling` — укладка домино как задача о двудольном паросочетании
- `GraphCenterMedian` — поиск центра и медианы ориентированного взвешенного графа
- `Kruskal` — алгоритм Краскала с системой непересекающихся множеств
- `Matchmaker` — задача о свахе (максимальное двудольное паросочетание)

---

## 🟡 greedy — жадные алгоритмы

Задачи, решаемые жадными стратегиями.

- `ExamSchedule` — задача о зачётной сессии (минимизация суммарного штрафа)
- `Hotel` — интервальное планирование заявок в гостинице

---

## 🔢 numbertheory — теория чисел и комбинаторика

Задачи, связанные с простыми числами, делимостью, комбинаторикой и диофантовыми уравнениями.

- `DiophantineSearch` — поиск натуральных решений диофантовых уравнений
- `KazanHappyTicket` — проверка казанского счастливого билета
- `MoscowPiterHappyTickets` — счастливые билеты по московскому и питерскому определениям
- `KazanHappyCount15` — точное количество 15-значных казанских билетов
- `PrimalityTests` — тест Ферма, тест Миллера–Рабина, числа Кармайкла

---

## 🔗 poset — частичные порядки и разбиения

Задачи, связанные с отношениями частичного порядка, теоремой Дилворта и разбиением на монотонные подпоследовательности.

- `QuadraticGraphs` — разбиение на минимальное число монотонных графиков за O(n²)
- `LogGraphs` — оптимальное разбиение на монотонные графики за O(n log n)

---

## 🧑‍💻 Язык и технологии

- Язык программирования: **Java**
- Используемые средства:
  - стандартная библиотека Java
  - `BigInteger` для работы с большими числами
  - стандартные структуры данных: `ArrayList`, `HashMap`, `PriorityQueue`, DSU и др.

---



## 📌 Примечание

Каждая задача реализовывалась отдельно с предварительным анализом сложности, корректности и выбора алгоритма.  
В пояснительных файлах приведены рассуждения и обоснования решений.
